# IBPDI — JSON Schema

Reserved for the JSON Schema derived from IBPDI, generated from the ontology. Empty until the derivation pipeline ships.

The native artifact under [`../native/`](../native/) is canonical; the normalized field inventory under [`../inventory/`](../inventory/) is the machine-readable substrate crosswalks reference today.
